Docker build for [Marker](https://github.com/fabiocolacio/Marker) a markdown editor.

Builds a deb package for Ubuntu.

```bash
export DEBFULLNAME="Jack the Ripper"
export DEBEMAIL="jack.ripper@email.com"
make
```
